name: Python CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [created]    
  workflow_dispatch:

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Black
        uses: dciborow/pyaction@0.0.18
        with:
          black: true

      - name: Bandit
        uses: dciborow/pyaction@0.0.18
        with:          
          bandit: true

      - name: Pylint
        uses: dciborow/pyaction@0.0.18
        with:
          pylint: true

      - name: Pyright
        uses: dciborow/pyaction@0.0.18
        with:          
          pyright: true

      - name: Flake8
        uses: dciborow/pyaction@0.0.18
        with:          
          flake8: true

  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Pytest
        uses: dciborow/pyaction@0.0.18
        with:          
          testing: true

      - name: Pytest
        uses: dciborow/pyaction@0.0.18
        with:          
          testing: true
          pytest_markers: '-m integration'

  publish:
    runs-on: ubuntu-latest    
    steps:
      - uses: actions/checkout@v2
      - if: ${{ github.event_name == 'release' }}
        name: Publish
        uses: dciborow/pyaction@0.0.18
        with:
          pypi_publish: true
          pypi_password: ${{ secrets.PYPI_PASSWORD }}
      - if: ${{ github.event_name != 'release' }}
        name: Publish
        uses: dciborow/pyaction@0.0.18
        with:
          test_publish: true
          pypi_password: ${{ secrets.TEST_PYPI_PASSWORD  }}
